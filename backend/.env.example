# Copy this file to .env and fill in your values
# These are only used as defaults; the setup wizard will save config.json
CLIENT_ID=
TENANT_ID=

# Anthropic API key — powers the AI features (device search, policy explainer, etc.)
# Get your key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
